name: WakeUpHarley
on:
  schedule:
    - cron: '0 */6 * * *'
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping
        run: curl -s https://your-app-name.streamlit.app/ > /dev/null
